import React from "react";
import HeroText from "./HeroText";

const Hero = () => {
  return (
    <main className="bg-home-mobile min-h-screen grid place-items-center px-home-hero bg-no-repeat bg-cover bg-center bg-fixed">
      <HeroText />
    </main>
  );
};

export default Hero;
